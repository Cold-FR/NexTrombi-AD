# grumphp.yml
grumphp:
    tasks:
        phpcsfixer:
            config: '.php-cs-fixer.dist.php'
            allow_risky: true
        phpstan:
            configuration: 'phpstan.dist.neon'
            memory_limit: "512M"
        twigcs: ~
        yamllint: ~
